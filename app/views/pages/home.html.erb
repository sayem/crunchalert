
<% if signed_in? %>

<%= render 'shared/crunchbase_form' %>

<% @alerts.each do |alert| %>

 <div class="crunchalert">
  <%= image_tag(Picture.find_by_content(alert.content)[:url]) %>
  <%=h alert.content.capitalize %>
  <%= link_to "Edit", "/", :class => "edit_alert" %>
 </div>

<% end %>

<% end %>
